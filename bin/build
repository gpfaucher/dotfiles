#! /usr/bin/env fish

switch (uname)
   case Linux
       sudo nixos-rebuild switch --flake .#nixbox
   case Darwin
       darwin-rebuild switch --flake .#nixbook
   case '*'
       echo "You're using an unsupported OS. Aborted build process."
end
